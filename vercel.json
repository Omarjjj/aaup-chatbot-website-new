{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "node build.js",
        "outputDirectory": "dist"
      }
    }
  ]
} 